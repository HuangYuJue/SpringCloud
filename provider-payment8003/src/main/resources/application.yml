#8003表示注册到zookeeper服务器的支付服务提供者的端口
server:
  port: 8003

#服务别名----注册zookeeper到注册中心名称
spring:
  application:
    name: cloud-provider-payment
  cloud:
    zookeeper:
      connect-string: 192.168.244.52:2181 #192.168.244.52由xshell中 ifconfig -a 可得
      #2181是zookeeper默认端口号

